name: Release action-plugins - port_checker

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
    paths:
      - "action-plugins/port_checker/**"

jobs:
  build-and-release:
    name: Build and Release port_checker
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Read Plugin Version
        id: read_version
        working-directory: action-plugins/port_checker
        run: |
          VERSION=$(cat .version)
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          
          # Check if pre-release
          if [[ "${VERSION}" =~ -(alpha|beta|rc|a|b)([0-9]*)?$ ]]; then
            echo "is_prerelease=true" >> $GITHUB_OUTPUT
          else
            echo "is_prerelease=false" >> $GITHUB_OUTPUT
          fi

      - name: Check if Tag or Release Exists
        id: check-tag-release
        env:
          GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        run: |
          TAG_EXISTS=$(git ls-remote --tags origin | grep "refs/tags/port_checker-v${{ steps.read_version.outputs.version }}" || true)
          RELEASE_EXISTS=$(gh release list --repo ${{ github.repository }} | grep "Release port_checker v${{ steps.read_version.outputs.version }}" || true)
          if [ -n "${TAG_EXISTS}" ] || [ -n "${RELEASE_EXISTS}" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.24'

      - name: Build Plugin
        if: steps.check-tag-release.outputs.skip == 'false'
        working-directory: action-plugins/port_checker
        run: |
          # Build for multiple platforms
          for os in darwin linux; do
            for arch in amd64 arm64; do
              GOOS=${os} GOARCH=${arch} go build -o port_checker-v${{ steps.read_version.outputs.version }}-${os}-${arch}
            done
          done
          
          # Build latest binaries (only for non-pre-releases)
          if [ "${{ steps.read_version.outputs.is_prerelease }}" = "false" ]; then
            for os in darwin linux; do
              for arch in amd64 arm64; do
                GOOS=${os} GOARCH=${arch} go build -o port_checker-latest-${os}-${arch}
              done
            done
          fi

      - name: Create Version Tag
        if: steps.check-tag-release.outputs.skip == 'false'
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.ACCESS_TOKEN }}
          custom_tag: port_checker-v${{ steps.read_version.outputs.version }}
          tag_prefix: ''
      
      - name: Update Latest Tag
        if: |
          steps.check-tag-release.outputs.skip == 'false' &&
          steps.read_version.outputs.is_prerelease == 'false'
        run: |
          set -e
          # Delete local and remote -latest tag if it exists
          git tag -d port_checker-latest 2>/dev/null || true
          git push origin :refs/tags/port_checker-latest 2>/dev/null || true
          # Create new -latest tag at current commit
          git tag port_checker-latest
          git push origin port_checker-latest --force

      - name: Create Version Release
        if: steps.check-tag-release.outputs.skip == 'false'
        id: create_version_release
        uses: ncipollo/release-action@v1
        with:
          name: Release port_checker v${{ steps.read_version.outputs.version }}
          tag: ${{ steps.tag_version.outputs.new_tag }}
          artifacts: action-plugins/port_checker/port_checker-v${{ steps.read_version.outputs.version }}-*
          skipIfReleaseExists: true
          generateReleaseNotes: true
          prerelease: ${{ steps.read_version.outputs.is_prerelease }}
          token: ${{ secrets.ACCESS_TOKEN }}
      
      - name: Create Latest Release
        if: |
          steps.check-tag-release.outputs.skip == 'false' &&
          steps.read_version.outputs.is_prerelease == 'false'
        id: create_latest_release
        uses: ncipollo/release-action@v1
        with:
          name: Release port_checker latest
          tag: port_checker-latest
          artifacts: action-plugins/port_checker/port_checker-latest-*
          skipIfReleaseExists: false
          generateReleaseNotes: false
          token: ${{ secrets.ACCESS_TOKEN }}
